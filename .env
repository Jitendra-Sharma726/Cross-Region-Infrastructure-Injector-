# Step 1: Initialize the working directory with provider plugins
./terraform init

# Step 2: Spin up multi-region buckets and map outputs.json
./terraform apply -auto-approve

# Step 3: Parse metrics and isolate regional configurations
ansible-playbook configure.yml
